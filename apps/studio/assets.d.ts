/** The directory holding Studio's built assets, as an absolute path. */
export declare const studioAssets: () => string

/** Where the published bundle expects to be mounted. */
export declare const STUDIO_BASE: '/studio'
