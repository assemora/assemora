# `@assemora/data`

Assemora Data: model(), query builder, relations, scopes, Query AST.

**Implementation phase:** 2 (see `SPEC.md` and `docs/architecture/roadmap.md`).

## Workspace dependencies

- `@assemora/schema`
- `@assemora/core`
- `@assemora/database`

Dependency direction is fixed in `docs/architecture/package-graph.md` and enforced by `pnpm boundaries`.
