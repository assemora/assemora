/**
 * `@assemora/cli` — Assemora CLI: generators, migrations, introspection.
 *
 * Implementation is scheduled for phase 10 (see SPEC.md). At phase 0 this package
 * carries only its boundary, public entry point and declared dependencies.
 */
export const PACKAGE = '@assemora/cli'
