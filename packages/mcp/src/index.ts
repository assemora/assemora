/**
 * `@assemora/mcp` — MCP server, agent API, dry run and change sets.
 *
 * Implementation is scheduled for phase 9 (see SPEC.md). At phase 0 this package
 * carries only its boundary, public entry point and declared dependencies.
 */
export const PACKAGE = '@assemora/mcp'
