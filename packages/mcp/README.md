# `@assemora/mcp`

MCP server, agent API, dry run and change sets.

**Implementation phase:** 9 (see `SPEC.md` and `docs/architecture/roadmap.md`).

## Workspace dependencies

- `@assemora/schema`
- `@assemora/core`
- `@assemora/resources`
- `@assemora/pages`

Dependency direction is fixed in `docs/architecture/package-graph.md` and enforced by `pnpm boundaries`.
